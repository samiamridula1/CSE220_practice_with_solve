
{
  "nbformat": 4,
  "nbformat_minor": 0,
  "metadata": {
    "colab": {
      "provenance": []
    },
    "kernelspec": {
      "name": "python3",
      "display_name": "Python 3"
    },
    "language_info": {
      "name": "python"
    }
  },
  "cells": [
    {
      "cell_type": "markdown",
      "source": [
        "Array\n",
        "Question 1(you are not allowed to use any built in function)\n",
        "\n",
        "Given two arrays a[] and b[] of size n and m respectively. The task is to find union between\n",
        "these two arrays.\n",
        "Union of the two arrays can be defined as the set containing distinct elements from both the\n",
        "arrays. If there are repetitions, then only one occurrence of element should be printed in the\n",
        "union"
      ],
      "metadata": {
        "id": "7n2BAAPyww5n"
      }
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {
        "id": "W7ydzYhxwtOU"
      },
      "outputs": [],
      "source": [
        "nm=input().split()\n",
        "n=int(nm[0])\n",
        "m=int(nm[1])\n",
        "\n",
        "a_str=input().split()\n",
        "a=[]\n",
        "for i in range(n):\n",
        "  a.append(int(a_str[i]))\n",
        "b_str=input().split()\n",
        "b=[]\n",
        "for i in range(m):\n",
        "  b.append(int(b_str[i]))\n",
        "u=[]\n",
        "for i in range(n):\n",
        "  f=False\n",
        "  for j in range(len(u)):\n",
        "    if u[j]==a[i]:\n",
        "      f=True\n",
        "      break\n",
        "    if f!=False:\n",
        "      u.append(a[i])\n",
        "for i in range(m):\n",
        "  f=False\n",
        "  for j in range(len(u)):\n",
        "    if u[j]==b[i]:\n",
        "      f=True\n",
        "      break\n",
        "  if f!=False:\n",
        "    u.append(b[i])\n",
        "print(len(u))"
      ]
    },
    {
      "cell_type": "markdown",
      "source": [
        "Given an unsorted array arr[] of size N having both negative and positive integers. The task is\n",
        "place all negative element at the end of array without changing the order of positive element\n",
        "and negative element.\n",
        "Example 1:\n",
        "Input :\n",
        "N = 8\n",
        "arr = [1, -1, 3, 2, -7, -5, 11, 6]\n",
        "Output :\n",
        "1 3 2 11 6 -1 -7 -5\n",
        "Example 2:\n",
        "Input :\n",
        "N=8\n",
        "arr =[-5, 7, -3, -4, 9, 10, -1, 11]\n",
        "Output :\n",
        "7 9 10 11 -5 -3 -4 -1"
      ],
      "metadata": {
        "id": "6vs6E_t0wt09"
      }
    },
    {
      "cell_type": "code",
      "source": [
        "n=int(input())\n",
        "arr_str=input().split()\n",
        "arr=[]\n",
        "for i in range(n):\n",
        " arr.append(int(arr_str[i]))\n",
        "pos=[]\n",
        "neg=[]\n",
        "for i in range(n):\n",
        "  if arr[i]>=0:\n",
        "    pos.append(arr[i])\n",
        "  else:\n",
        "    neg.append(arr[i])\n",
        "res=[]\n",
        "for i in range(len(pos)):\n",
        "  res.append(pos[i])\n",
        "for i in range(len(neg)):\n",
        "  res.append(neg[i])\n",
        "for i in range(len(res)):\n",
        "    print(res[i],end=\" \")\n"
      ],
      "metadata": {
        "id": "yDzVvxj3y3I7"
      },
      "execution_count": null,
      "outputs": []
    },
    {
      "cell_type": "markdown",
      "source": [
        "Question 4\n",
        "Given an array of N positive integers and an integer X. The task is to find the frequency of X in\n",
        "the array.\n",
        "Example 1:\n",
        "Input:\n",
        "N = 5\n",
        "arr = [1, 1, 1, 1, 1]\n",
        "X = 1\n",
        "Output:\n",
        "5\n",
        "Explanation: The frequency of 1 is 5.\n"
      ],
      "metadata": {
        "id": "EgL2MkXUubhz"
      }
    },
    {
      "cell_type": "code",
      "source": [
        "n1=int(input())\n",
        "a1=input().split()\n",
        "a=[]\n",
        "for i in range(n1):\n",
        "  a.append(int(a1[i]))\n",
        "x=int(input())\n",
        "c=0\n",
        "for j in range(n1):\n",
        "  if a[j]==x:\n",
        "    c+=1\n",
        "print(f\"The frquncy of{x}is {c}\")"
      ],
      "metadata": {
        "colab": {
          "base_uri": "https://localhost:8080/"
        },
        "id": "qXBo17lrudiy",
        "outputId": "b3dcab09-703b-4105-a1aa-2547fa85a2c3"
      },
      "execution_count": 12,
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "5\n",
            "1 1 1 1 1\n",
            "1\n",
            "The frquncy of1 is 5\n"
          ]
        }
      ]
    },
    {
      "cell_type": "markdown",
      "source": [
        "Question 5\n",
        "Given an array arr and an integer K where K is smaller than size of array, the task is to find the\n",
        "Kth smallest element in the given array. It is given that all array elements are distinct.\n",
        "Example 1:\n",
        "Input:\n",
        "N = 6\n",
        "arr = [7, 10, 4, 3, 20, 15]\n",
        "K = 3\n",
        "Output : 7\n",
        "Explanation :\n",
        "3rd smallest element in the given\n",
        "array is 7.\n",
        "Example 2:\n",
        "Input:\n",
        "N = 5\n",
        "arr = [7, 10, 4, 20, 15]\n",
        "K = 4\n",
        "Output : 15\n",
        "Explanation :\n",
        "4th smallest element in the given\n",
        "array is 15"
      ],
      "metadata": {
        "id": "lKa0PtvtxwyP"
      }
    },
    {
      "cell_type": "code",
      "source": [
        "n=5\n",
        "a=\"7 10 4 3 20 15\".split()  #hardcoded\n",
        "a1=[]\n",
        "for i in range(n):\n",
        "  a1.append(int(a[i]))\n",
        "print(a1)\n",
        "k=3\n",
        "for i in range(n):\n",
        " for j in range(i+1,n):\n",
        "   if a1[i]>a1[j]: #if not allowed to use another list\n",
        "    tmp=a1[i]\n",
        "    a1[i]=a1[j]\n",
        "    a1[j]=tmp\n",
        "print(a1)"
      ],
      "metadata": {
        "colab": {
          "base_uri": "https://localhost:8080/"
        },
        "id": "wtjy6gKDxyrk",
        "outputId": "822df76d-6caf-48db-ad4d-383aca9e43cc"
      },
      "execution_count": 27,
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "[7, 10, 4, 3, 20]\n",
            "[3, 4, 7, 10, 20]\n"
          ]
        }
      ]
    }
  ]
}
